---
- hosts: localhost
  connection: local
  name: Parsing a CSV file into A variable file 

  tasks:
    - name: Parsing a CSV file into A variable file
      parsing_csv_into_ansible_vars:
        csv_file: ../roles/pagerduty/files/pagerduty_user_creation_data.csv
        yaml_file: ../roles/pagerduty/vars/pagerduty_user_creation_data.yml